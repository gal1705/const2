<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
  <div class="navbar-brand">Construcción</div>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item  active">
        <a class="nav-link" href="<%= projects_path %>">Proyectos</a>
      </li>
      <li class="nav-item  active dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Empresas</a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="<%= recompanies_path %>">Inmobiliarias</a>
          <a class="dropdown-item" href="<%= companies_path(category: 'Constructoras') %>">Constructoras</a>
          <a class="dropdown-item" href="<%= companies_path(category: 'Arquitectos') %>">Arquitectos</a>
          <a class="dropdown-item" href="<%= companies_path(category: 'Ingenieros') %>">Ingenieros</a>
        </div>
      </li>
      <li class="nav-item  active dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <% if company_signed_in? || recompany_signed_in? %>
          Cerrar Sesión
        <% else %>
          Acceso
        <% end %>
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <% if company_signed_in? %>
            <a class="dropdown-item" href="<%= destroy_company_session_path %>" data-method="DELETE">Salir (Company)</a>
          <% elsif recompany_signed_in? %>
            <a class="dropdown-item" href="<%= destroy_recompany_session_path %>" data-method="DELETE">Salir (recompany)</a>

          <% else %>
            <a class="dropdown-item" href="<%= new_recompany_session_path %>">Ingreso Inmobiliarias</a>
            <a class="dropdown-item" href="<%= new_recompany_registration_path %>">Registro Inmobiliarias</a>
            <a class="dropdown-item" href="<%= new_company_session_path %>">Ingreso Empresas</a>
            <a class="dropdown-item" href="<%= new_company_registration_path %>">Registro Empresas</a>
          <% end %>
        </div>
      </li>
    </ul>
  </div>
</nav>